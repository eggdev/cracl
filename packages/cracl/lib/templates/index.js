const components = require('./_components');
const context = require('./_context');
const hooks = require('./_hooks');
const index = require('./_index');
const storybook = require('./_storybook');

module.exports = {
  routes: components,
  components,
  context,
  hooks,
  index,
  storybook,
};
